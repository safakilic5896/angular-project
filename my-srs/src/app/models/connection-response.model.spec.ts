import { ConnectionResponse } from './connection-response.model';

describe('ConnectionResponse.Model', () => {
  it('should create an instance', () => {
    expect(new ConnectionResponse()).toBeTruthy();
  });
});
